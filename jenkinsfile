@Library('Todoshop') _

todoshop (
        COMPONENT             : 'login',
        PROJECT_NAME          : "Todoshop",
        SLAVE_LABEL           : "node",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)
